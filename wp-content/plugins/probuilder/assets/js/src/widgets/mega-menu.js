export default // Widget renderer for "mega-menu" (auto-generated)
function renderMegaMenu(context) {
  const {
    element,
    settings,
    spacingStyles,
    app,
    depth = 0,
    widget
  } = context;
}